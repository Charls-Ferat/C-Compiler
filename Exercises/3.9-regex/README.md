# Exercise 3.9 – Regular Expression Testing

This directory contains Python code to test regular expressions
defined for exercise 3.9 using Python's `re` module.

Each regex is tested against:
- strings that must match
- strings that must not match
